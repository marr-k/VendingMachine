describe('Use Cases', () => {
  describe('Use Case 1', () => {
    it('An Admin loads a VendingMachine with 5 Twixes (price: $.75 each), 10 Sour Patch Kids (price: $2.00 each), and 3 Atomic Warheads (price: $.50 each)', () => {
      expect(true).toBe(false);
    });
  });
});
